# .github

## Github CI dependencies
If a project is using browserstack and not local browsers, it must upgrade to karma@5 and videojs-generate-karma-config@7.
The .nvmrc also needs to be udpated to use a number instead of codenames.
